#include "dialog.hpp"

namespace Gameplay{
/*	Dialog::Dialog(MessageBox& _messageBox, Controller& _self, Controller& _other)
		: m_messageBox(_messageBox)
	{
		m_self = _self;
		m_other = _other;

		Init();
	}

	// ********************************************* //

	void Dialog::Say(Controller& _source, const std::string& _text)
	{
//		m_messageBox.DisplayMsg(_source.GetName() + ": " + _text, _text.length() * (2.f / 14.f));
	}*/
}